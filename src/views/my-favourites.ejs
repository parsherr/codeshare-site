<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Likes</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.0.2/dist/tailwind.min.css">
  <style>
    .search-css {
      background-color: #f3f4f6;
      border: 1px solid #d1d5db;
      color: #1f2937;
    }
  </style>
</head>
<body>

  <%- include('partials/_header'); -%>
  <%- include('partials/_loader'); -%>

  <div class="container mx-auto py-10">
    <h1 class="text-4xl text-white text-center">Favori Kodlar</h1>
    <br />


    <br />
    <br />

    <h1 class="text-white text-center" style="font-size: 1.25rem;">favori kodlarım : <%= codes.length %></h1>
    <div class="bg-white dark:bg-zinc-900 pt-5 container mx-auto px-4 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 mb-auto">
      <% if (codes.length === 0) { %>
        <div class="flex items-center justify-center w-full h-64 bg-white dark:bg-gray-800 dark:text-gray-400 text-gray-700 rounded-lg shadow">
          Henüz favori code sahip değilsiniz.
        </div>
      <% } %>
      <% codes.forEach(code => { %>
        <div class="cursor-pointer flex flex-col h-full p-6 bg-white border border-gray-200 rounded-lg shadow hover:bg-gray-100 dark:bg-gray-800 dark:border-gray-700 dark:hover:bg-gray-700">
          <a class="flex flex-col h-full" href="/codes/<%= code.id %>">
            <h5 class="mb-2 text-xl font-bold tracking-tight text-gray-900 dark:text-white"><%= code.name %></h5>
            <p class="font-normal text-gray-700 dark:text-gray-400 mb-5"><%= code.desc %></p>
          </a>
        </div>
      <% }); %>
    </div>

    <h1 class="text-white text-center" style="font-size: 1.25rem;">favori repolarım : <%= repos.length %></h1>

    <div class="bg-white dark:bg-zinc-900 pt-5 container mx-auto px-4 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 mb-auto">
      <% if (repos.length === 0) { %>
        <div class="flex items-center justify-center w-full h-64 bg-white dark:bg-gray-800 dark:text-gray-400 text-gray-700 rounded-lg shadow">
          Henüz favori repoya sahip değilsiniz.
        </div>
      <% } %>
      <% repos.forEach(repo => { %>
        <div class="cursor-pointer flex flex-col h-full p-6 bg-white border border-gray-200 rounded-lg shadow hover:bg-gray-100 dark:bg-gray-800 dark:border-gray-700 dark:hover:bg-gray-700">
          <a class="flex flex-col h-full" href="/repository/<%= repo.name %>">
            <h5 class="mb-2 text-xl font-bold tracking-tight text-gray-900 dark:text-white"><%= repo.name %></h5>
            <p class="font-normal text-gray-700 dark:text-gray-400 mb-5"><%= repo.description %></p>
          </a>
        </div>
      <% }); %>

        


  </div>

  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.0/jquery.min.js"></script>
  <script>
    $(document).ready(function () {
      $("#search").keyup(function () {
        var value = $(this).val().toLowerCase();
        $(".cursor-pointer").filter(function () {
          $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1);
        });
      });
    });
  </script>

  <script>
    if (localStorage.getItem("mode") != "") {
      document.body.classList.add(localStorage.getItem("mode"));
      document.getElementById("mode-btn").checked = true;
    }
  </script>
  <script src="https://code.jquery.com/jquery-3.5.0.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/js/bootstrap.bundle.min.js"></script>

  <%- include('partials/_footer'); -%> 
  <%- include('partials/_upbtn'); -%>

</body>
</html>
